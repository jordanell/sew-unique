<section class="gallery-list-item">
  <h2>
    <%= gallery.title %>
  </h2>
  <% gallery.images.each do |image| %>
    <%= render partial: 'images/list_item', locals: { image: image } %>
  <% end %>
</section>
